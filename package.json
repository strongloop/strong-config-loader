{
  "name": "strong-config-loader",
  "description": "recursively load config files",
  "version": "0.0.4",
  "scripts": {
    "blanket": {
      "pattern": "//^((?!(node_modules|test)).)*$/"
    },
    "coverage": "mocha -r blanket -R html-cov > coverage_strong-config-loader.html",
    "test": "mocha"
  },
  "license": "MIT",
  "dependencies": {
    "debug": "latest",
    "strong-task-emitter": "~0.0.1",
    "deepmerge": "~0.2.5"
  },
  "devDependencies": {
    "mocha": "latest",
    "blanket": "latest"
  },
  "repository" : {
    "type" : "git",
    "url" : "http://github.com/strongloop/strong-config-loader.git"
  }
}
