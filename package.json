{
  "name": "strong-config-loader",
  "description": "recursively load config files",
  "version": "0.0.4",
  "scripts": {
    "coverage": "istanbul cover node_modules/.bin/_mocha && istanbul report cobertura",
    "ci-test": "mocha -R xunit > xunit.xml",
    "test": "mocha"
  },
  "license": "MIT",
  "dependencies": {
    "debug": "latest",
    "strong-task-emitter": "~0.0.1",
    "deepmerge": "~0.2.5"
  },
  "devDependencies": {
    "mocha": "~1.13.0",
    "istanbul": "~0.1.44"
  },
  "repository": {
    "type": "git",
    "url": "http://github.com/strongloop/strong-config-loader.git"
  }
}
